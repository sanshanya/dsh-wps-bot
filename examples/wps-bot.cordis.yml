# WPS 365 通道插件挂载示例——cordis patch 同构（用法与 ../../cordis.patch.yml 一致）
# 拷贝进你的组合或 profile 层即可装载;`name` 指头被裁判过的源码原件
- insert:
    - id: wps-bot
      name: ../../dsh-wps-bot/src/index.ts
      disabled: false
      config: {}
