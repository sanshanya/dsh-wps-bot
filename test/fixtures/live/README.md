# 真帧证据位（AGENT_RULES:21）

本目录的**真机 JSONL 帧**是评审造的固定钉。规则：真实捕获，不复印刷 markdown 文档。

当前：首帧抓包本墨证据于 `docs/acceptance-live.md` 附录（时间戳行）——那是唯一存证位。

补充新真帧时：扔本目录 `<场景>.jsonl`，日戳行写入 acceptance-live.md;**禁止把 markdown 拷贝过来当帧**（本仓 c-z3 督办:“1216 字 markdown 当帧不算证据”）。
